#pragma once

#include <algorithm>
#include <cmath>
#include <limits>
#include <stdexcept>
#include "def.h"

namespace Mlib::Math {
    FORCE_INLINE UNUSED auto between(double a, double b, double x) -> double;
}
